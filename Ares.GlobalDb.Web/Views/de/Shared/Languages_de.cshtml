@model LanguageResponse
           
@if (Model.GetDifferentLanguageUrls().Count > 0)
{
    <p>
        Andere Datenbasis-Sprachen durchsuchen: 
        @foreach (var language in Model.GetDifferentLanguageUrls())
        {
            <a href="@language.Url">@language.Name</a> 
        }
    </p>
}